public class SearchAlgs {
    public static int binarySearch(int[] ar,int element){
        return binarySearch(ar,0,ar.length-1,element);
    }
    private static int binarySearch(int[] ar,int left,int right, int element) {
        if(left<=right) {

        }

        return -1;
    }
}
